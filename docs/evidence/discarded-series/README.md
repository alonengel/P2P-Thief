# Discarded first attempt (2026-08-03) — dead-end evidence, kept on purpose

This folder archives the abandoned first series attempt against `imreeyal`, preserved
under the project's evidence guardrail ("archived on both sides, never deleted" — logs
are the only accepted dispute evidence). Nothing in the counted series depends on it.

The two repos' copies of this attempt **differ, and that is provenance, not a dispute**
(finding R-4 of imreeyal's reciprocal audit, 2026-08-05, disposition agreed on
league-kit issue #37):

- **The sibling P2P-Police copy is the later, authoritative snapshot**: all six
  windows, the current row schema (`score` / `result` / `log_files`), and
  `game_uid 2f0c25a9-5008-03c7-2d60-645dd51be11a`.
- **This (Thief) copy is an earlier writer's snapshot**: four of six windows, the
  pre-hardening row schema (`scores` / `outcome` / `log_file`), and no `game_uid` —
  that field did not exist in the writer yet. It is kept unmodified because editing
  historical dispute evidence to make it tidier would defeat the folder's purpose.

No `config_*.json` is archived here; the terms in force were byte-identical to the
counted constitution archived in `config/games/`. The counted series itself
(`game_uid e351176a-…`, results/) derives nothing from this folder.
